{
  "name": "english-platform-overhaul-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only test harness for the kids-stories platform. Nothing here ships to Firebase Hosting (see firebase.json ignore of **/node_modules/** and package files).",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "fast-check": "^3.23.1",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "firebase": "^12.15.0"
  }
}
